rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For this prototype, allow all reads and writes.
      // In production, you would restrict this to authenticated users.
      allow read, write: if true;
    }
  }
}
